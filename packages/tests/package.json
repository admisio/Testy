{
    "name": "@testy/tests",
    "main": "./index.ts",
    "types": "./index.ts",
    "scripts": {
        "migrate:postgres": "dotenv -e .env.test -- npx prisma migrate deploy --schema ../database/prisma/schema.prisma",
        "test": "dotenv -e .env.test vitest"
    },
    "dependencies": {
        "@testy/database": "workspace:*",
        "@testy/trpc": "workspace:*",
        "@trpc/client": "^10.12.0",
        "bcrypt": "^5.1.0",
        "dotenv": "^16.0.3",
        "dotenv-cli": "^7.0.0",
        "vitest": "^0.28.5"
    },
    "devDependencies": {
        "@types/bcrypt": "^5.0.0",
        "ts-node": "^10.9.1",
        "vitest-mock-extended": "^1.0.9"
    },
    "prisma": {
        "seed": "node --loader ts-node/esm prisma/seed.ts"
    }
}